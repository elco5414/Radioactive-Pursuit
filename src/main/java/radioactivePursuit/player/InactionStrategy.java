package radioactivePursuit.player;

public class InactionStrategy implements PlayStrategy{

    @Override
    public void doAction(Player scientist) {
        //will do nothing, the player has not chosen an active move
    }
}
